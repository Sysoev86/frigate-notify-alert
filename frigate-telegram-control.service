[Unit]
# __APPDIR__ is replaced with the real install path by `./manage.sh install`.
Description=Frigate Telegram Monitor - Mute Controller (pause buttons)
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=__APPDIR__
ExecStart=/bin/bash -c "source __APPDIR__/venv/bin/activate && python __APPDIR__/mute_controller.py"
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
